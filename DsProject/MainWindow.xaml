<Window x:Class="DsProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:DsProject.MWM.ViewModel"
        xmlns:local="clr-namespace:DsProject"
        mc:Ignorable="d"
        Height="600" Width="920"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        >

    <Window.DataContext>
        <viewModel:MainViewModel />
    </Window.DataContext>

    <Border Background="#272537" CornerRadius="20">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="75" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Text="FileExplorer" FontFamily="/Fonts/#Poppins" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="22, 0 , 0 ,0" Foreground="White" FontSize="22" />

            <StackPanel x:Name="dynamicVolumes" Grid.Row="1">
                <RadioButton Content="Home" Height="50" Foreground="white" FontSize="14" IsChecked="True" Style="{StaticResource MenuButtomTheme}" Command="{Binding HomeViewCommand}" />
                <RadioButton Content="Discovery" Height="50" Foreground="white" FontSize="14" Style="{StaticResource MenuButtomTheme}" Command="{Binding DiscoveryViewCommand}" />
                <RadioButton Content="Featured" Height="50" Foreground="white" FontSize="14" Style="{StaticResource MenuButtomTheme}" />
            </StackPanel>
            <StackPanel Grid.Column="1"  VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBox x:Name="txtSearch"  Width="250" Height="40" VerticalContentAlignment="Center" Margin="5" HorizontalAlignment="Left"  Style="{StaticResource ModernTextbox}" />
                <Button x:Name="btnClose" Content="X" Width="32" Height="26" Margin="5" VerticalAlignment="Top" BorderBrush="Transparent" Background="Transparent" Foreground="White" Click="btnClose_Click" />
            </StackPanel>
            <ContentControl Grid.Row="1"
                            Grid.Column="1"
                            Margin="10" Content="{Binding CurrentView}"/>
        </Grid>


    </Border>
</Window>
    